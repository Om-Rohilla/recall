name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

# ─────────────────────────────────────────────────────────────────────────────
# Job 1: Build Linux binary on a native Ubuntu runner
# Uses --split so GoReleaser writes metadata for goreleaser continue --merge
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  build-linux:
    name: Build (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"
          cache: true

      - name: Install CGO dependencies
        run: sudo apt-get update && sudo apt-get install -yq --no-install-recommends gcc libsqlite3-dev

      - uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --clean --split --skip=publish,announce,validate
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/upload-artifact@v4
        with:
          name: dist-linux
          path: dist/
          retention-days: 1

# ─────────────────────────────────────────────────────────────────────────────
# Job 2: Build macOS binaries on a native macOS runner
# Uses --split so GoReleaser writes metadata for goreleaser continue --merge
# ─────────────────────────────────────────────────────────────────────────────
  build-macos:
    name: Build (macOS)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"
          cache: true

      # Xcode Command Line Tools provide clang — no extra install needed.
      - uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --clean --split --skip=publish,announce,validate
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/upload-artifact@v4
        with:
          name: dist-macos
          path: dist/
          retention-days: 1

# ─────────────────────────────────────────────────────────────────────────────
# Job 3: Merge all platform artifacts and publish the GitHub Release
# goreleaser continue --merge reads the metadata from all dist/ folders and
# creates archives, checksums, GitHub Release and the Homebrew formula.
# ─────────────────────────────────────────────────────────────────────────────
  release:
    name: Release
    needs: [build-linux, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"
          cache: true

      - uses: actions/download-artifact@v4
        with:
          name: dist-linux
          path: dist/

      - uses: actions/download-artifact@v4
        with:
          name: dist-macos
          path: dist/

      - name: Install CGO dependencies (needed for goreleaser continue)
        run: sudo apt-get update && sudo apt-get install -yq --no-install-recommends gcc libsqlite3-dev

      - uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: continue --merge
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_PAT: ${{ secrets.GH_PAT }}
